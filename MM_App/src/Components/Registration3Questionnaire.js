import React from 'react';
import { connect } from 'react-redux';
import { View } from 'react-native';

import { CardSection, Input, Button } from './common';

class Reg3 extends React.Component {
  render() {
    return (
      <View>
        <CardSection>
          <Button>
            button
          </Button>
        </CardSection>
      </View>
    );
  }
}

export default Reg3;
